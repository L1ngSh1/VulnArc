# AI 辅助研究

```text
人类直觉 → 独立提出假设 → AI 独立分析 → 对比
→ 质疑既有假设 → 人工代码审查 → 运行时复现
→ 已验证 / 已否定 → 提炼模式 → 改进人类直觉
```

人类研究员是最终的安全判断者。AI 可充当代码导航助手、假设生成器、对抗性审查者、推理助手和攻击面扩展工具。AI 输出只是证据线索，绝不是安全结论。应在相关情况下记录模型与测试框架的详细信息，但不要让数据结构绑定到某一家提供商。

# Research Workflow

`hypothesis → candidate → validated → reported → embargoed/disclosed → public` or `candidate → rejected`. Invalid shortcuts are rejected. Rejected records are retained: document why the idea looked vulnerable, the disproving evidence, the missed control, and the intuition to update. Reopening is intentionally not automated in v0.1 and requires a documented manual review.

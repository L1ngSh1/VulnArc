# Contributing

Use Python 3.12+, small typed modules, readable Markdown/YAML, and tests. Run `pytest` and `ruff check .`. Synthetic examples must use fictional projects. Never submit undisclosed findings, secrets, private reproduction evidence, or claims about unverified vulnerabilities.

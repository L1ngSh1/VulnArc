# Gitea (safe example target)

This target contains architecture prompts only. It makes no vulnerability claim. Research should use an authorized local instance and pin a version/commit before analysis.
